/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['https://i.dummyjson.com/', 'dummyimage.com', "i.dummyjson.com"],
  },
}

module.exports = nextConfig
